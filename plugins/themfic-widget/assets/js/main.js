jQuery(document).ready(function($) {
  // Initialize Slick Slider
  $('.slick-slider').slick({
      dots: true,
      infinite: true,
      speed: 500,
      slidesToShow: 1,
      adaptiveHeight: true,
      // Add more Slick options as needed
  });
});